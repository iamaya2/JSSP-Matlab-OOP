% Parameter initialization
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [50];% 30 50] %number of particles for UPSO
selfconf = [2.5]; %Self Confidence Factor to evaluate 
globalconf = [1.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [1 4 3 2]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 1; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
% Parameter initialization
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [10];% 30 50] %number of particles for UPSO
selfconf = [1.5]; %Self Confidence Factor to evaluate 
globalconf = [2.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [1 4 3 2]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 2; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
                        
% Parameter initialization
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [10];% 30 50] %number of particles for UPSO
selfconf = [1.5]; %Self Confidence Factor to evaluate 
globalconf = [2.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [2 4 3 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 1; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);

% Parameter initialization
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [10];% 30 50] %number of particles for UPSO
selfconf = [2.5]; %Self Confidence Factor to evaluate 
globalconf = [0.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.9]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [2 4 3 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 2; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);  

nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [10];% 30 50] %number of particles for UPSO
selfconf = [0.5]; %Self Confidence Factor to evaluate 
globalconf = [2.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [3 4 2 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 1; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
                        
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [30];% 30 50] %number of particles for UPSO
selfconf = [2.5]; %Self Confidence Factor to evaluate 
globalconf = [1.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [3 4 2 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 2; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
                        
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [30];% 30 50] %number of particles for UPSO
selfconf = [2.5]; %Self Confidence Factor to evaluate 
globalconf = [1.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [4 3 2 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 1; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
                        
                        
nbJobsV = [3]; %number of jobs desired
nbMachinesV = [4]; %number of machines desired
population = [50];% 30 50] %number of particles for UPSO
selfconf = [0.5]; %Self Confidence Factor to evaluate 
globalconf = [1.5]; %Global Confidence Factor to evaluate
unifyfactor = [0.1]; %Unifying Factor 
nbRep = 1; %Number of instances per combination
heurID = [4 3 2 1]; %Number: 1.LPT 2.SPT 3.MPA 4.LPA ; first the heuristic to worsen, second the heuristic to improve
timeRanges = [0 10]; % Min,Max processing times for the instance
objective = 2; % 1.- Enhance heuristic performance 2.- Diminish heuristic performance
delta = 10; % optional; needs to be added to the parameters (always a positive number)
toPlot = false; %optional; needs to be added to the parameters
folder = "Instances2905";
TaskManagerPrueba_Advanced(nbJobsV,nbMachinesV,timeRanges,population,selfconf, globalconf,...
                            unifyfactor, nbRep, heurID, objective, folder);
                                     
                                     